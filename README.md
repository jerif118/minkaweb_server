# Minka Web Server

This project contains the WebSocket server for Minka.

## Running the server

Change into the `minka_plantilla/server` directory and start the application with:

```bash
python run.py
```

`run.py` is the canonical entry point and replaces the old `run_server.py` script.
